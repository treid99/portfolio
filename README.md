# Portfolio

Personal portfolio repository.

## Status

Scaffolding — stack not yet chosen.

## Local setup notes

This repo uses a **repo-local git identity** so it stays separate from any
work account configured globally on this machine:

| Setting | Value |
| --- | --- |
| `user.name` | Tom Reid |
| `user.email` | `treid99@users.noreply.github.com` |
| `credential.username` | `treid99` |
| `origin` | `https://treid99@github.com/treid99/portfolio.git` |

Verify before committing:

```sh
git config user.email   # -> treid99@users.noreply.github.com
```
